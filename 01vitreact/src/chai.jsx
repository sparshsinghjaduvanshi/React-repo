function Chai(){
    return (
        <h2>chai is ready</h2>
    )
} //function name's first character should be in capital letter otherwise it will not work as a component. It will be treated as a normal html tag and will not render anything on the screen.

export default Chai;